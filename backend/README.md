# ContactoUshuaiaApi
Api pagina

# Ejecutar: uvicorn main:app --reload
# Crear archivo requirements: pip freeze > requirements.txt
# Instalar las dependencias: pip install -r requirements.txt
# Docker: docker run -p 8000:8000 contacto-backend

# Puerto 3000